public with sharing class Filme extends Titulo {

    // CONSTRUTOR
    // Sinaliza que o construtor padrão de filme é o mesmo da classe super (Título), em que o nome é obrigatório.
        public Filme(String nomeInput){
            super(nomeInput);
        }

}